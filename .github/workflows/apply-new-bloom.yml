name: Apply New Bloom Upload

on:
  push:
    branches:
      - staging/new-bloom-upload

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout staging payload
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect ready payload
        id: payload
        shell: bash
        run: |
          if [ -f new-bloom-ready.txt ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Reassemble and verify payload
        if: steps.payload.outputs.present == 'true'
        shell: bash
        run: |
          cat new-bloom-delta.part-* > /tmp/new-bloom-delta.b64
          base64 -d /tmp/new-bloom-delta.b64 > /tmp/new-bloom-delta.tar.xz
          echo "0f37b57795b4b4d0e05dc21e707b1b5ae47349c8fac72772511896ba6905dbe3  /tmp/new-bloom-delta.tar.xz" | sha256sum -c -

      - name: Build sanitized New Bloom tree
        if: steps.payload.outputs.present == 'true'
        shell: bash
        run: |
          git checkout --detach 217485f6701366f3cd4307d202e6c635d69021fb
          tar -xJf /tmp/new-bloom-delta.tar.xz
          git status --short

      - name: Clean install
        if: steps.payload.outputs.present == 'true'
        run: npm ci

      - name: Run Bloom tests
        if: steps.payload.outputs.present == 'true'
        run: npm test

      - name: Typecheck
        if: steps.payload.outputs.present == 'true'
        run: npm run typecheck

      - name: Build web
        if: steps.payload.outputs.present == 'true'
        run: npm run build:web

      - name: Commit and fast-forward main
        if: steps.payload.outputs.present == 'true'
        shell: bash
        run: |
          git config user.name "Bloom Build Bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Apply new Bloom screens and Strength baseline"
          git push origin HEAD:main
