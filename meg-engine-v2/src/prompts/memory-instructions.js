const MEMORY_PROMPT = `Use memory quietly and only when it is relevant. Treat stored observations as user-reported facts, not diagnoses. Do not mention the memory system. Do not let an old observation override what the user says now.`;
module.exports = { MEMORY_PROMPT };
